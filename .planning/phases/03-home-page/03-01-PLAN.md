---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/homeContent.ts
  - src/components/Hero.astro
  - src/components/StatsBar.astro
  - src/components/ServicesGrid.astro
  - public/images/hero.jpg
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-07]

must_haves:
  truths:
    - "Hero section displays headline, subtitle, body copy, yellow CTA button, and a visible (non-broken) photo"
    - "Stats bar shows 300+, 100+, 10+, 150 with count-up animation triggered by IntersectionObserver"
    - "Stats bar shows correct numbers even with JavaScript disabled (fallback text content)"
    - "Services grid displays 6 cards (Customer Support, Real Estate, Healthcare, Tech Support, Sales, Legal) with icons and descriptions"
    - "All section text content lives in src/data/homeContent.ts, not hardcoded in component markup"
  artifacts:
    - path: "src/data/homeContent.ts"
      provides: "Typed content data for all home page sections"
      exports: ["stats", "services", "valueProps", "processSteps", "testimonials"]
    - path: "src/components/Hero.astro"
      provides: "Hero section with photo, headline, CTA"
      min_lines: 20
    - path: "src/components/StatsBar.astro"
      provides: "Stats bar with count-up animation"
      contains: "IntersectionObserver"
    - path: "src/components/ServicesGrid.astro"
      provides: "6 service cards in responsive grid"
      min_lines: 15
    - path: "public/images/hero.jpg"
      provides: "Hero background photo"
  key_links:
    - from: "src/components/StatsBar.astro"
      to: "src/data/homeContent.ts"
      via: "import { stats }"
      pattern: "import.*stats.*from.*homeContent"
    - from: "src/components/ServicesGrid.astro"
      to: "src/data/homeContent.ts"
      via: "import { services }"
      pattern: "import.*services.*from.*homeContent"
    - from: "src/components/StatsBar.astro"
      to: "IntersectionObserver"
      via: "script tag with data-count elements"
      pattern: "data-count"
    - from: "src/components/Hero.astro"
      to: "public/images/hero.jpg"
      via: "img src"
      pattern: "/images/hero\\.jpg"
---

<objective>
Build the home page data layer and first three section components: Hero (with stock photo, headline, yellow CTA to Calendly), StatsBar (4 stats with IntersectionObserver count-up animation), and ServicesGrid (6 industry cards). Also source and place the hero photo.

Purpose: These are the above-the-fold and immediate-scroll sections that establish credibility within the first 3 seconds of a visitor landing on the page. The data file created here is shared by all sections in Plan 02.

Output: homeContent.ts (complete data for all 6 sections), 3 Astro components, hero photo asset
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-RESEARCH.md

@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/components/Header.astro
@src/pages/index.astro

<interfaces>
<!-- Brand tokens available in global.css @theme {} -->
Colors: bg-navy, text-navy, bg-blue, text-blue, bg-teal, text-teal, bg-teal-light, bg-yellow, text-yellow
Fonts: font-heading (Open Sauce One), font-body (Inter)

<!-- BaseLayout.astro props -->
From src/layouts/BaseLayout.astro:
Props: { title: string; description?: string }
Slot: default slot for page content

<!-- Established patterns from Phase 2 -->
Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
External links: target="_blank" rel="noopener"
Calendly URL: https://calendly.com/workwiser-info/ceo-client
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homeContent.ts data file and source hero photo</name>
  <files>src/data/homeContent.ts, public/images/hero.jpg</files>
  <action>
Create `src/data/` directory and `src/data/homeContent.ts` with ALL home page content for all 6 sections. This data file is the single source of truth — Plan 02 components will also import from it.

**TypeScript interfaces and exports (all required):**

1. `Stat` interface + `stats` array (4 items):
   - { value: 300, suffix: '+', label: 'Completed Projects' }
   - { value: 100, suffix: '+', label: 'Satisfied Clients' }
   - { value: 10, suffix: '+', label: 'Years Experience' }
   - { value: 150, suffix: '', label: 'Team Members' }

2. `ServiceCard` interface + `services` array (6 items):
   - Customer Support, Real Estate, Healthcare, Tech Support, Sales, Legal
   - Each with: title, description (1-2 professional sentences), iconPath (SVG `d` attribute string for a relevant icon)
   - Icon SVGs should use a 24x24 viewBox (standard icon size)

3. `ValueProp` interface + `valueProps` array (6 items):
   - Simplified Hiring, IRS Compliance, Full Contract Management, Customized Roles, Bilingual Talent, Flexible Payroll Options
   - Each with: title, description (1-2 sentences explaining the benefit), iconPath (SVG `d` attribute string)

4. `ProcessStep` interface + `processSteps` array (5 items):
   - Step 1: Understanding Your Needs
   - Step 2: Pre-Qualified Candidates
   - Step 3: Client Selection
   - Step 4: Quick Onboarding
   - Step 5: Boost Productivity
   - Each with: step number, title, description (1 sentence explaining the step)

5. `Testimonial` interface + `testimonials` array (3 items):
   - Placeholder testimonials that sound realistic for a VA/staffing agency
   - Each with: quote (2-3 sentences), name, title (role + company), initials (2 chars)
   - Names and companies should be clearly fictional but professional-sounding

**Hero photo:**
- Create `public/images/` directory
- Download a professional stock photo from Unsplash depicting a virtual assistant or remote professional at a desk
  - Use curl to download: `curl -L "https://images.unsplash.com/photo-1573497019940-1c28c88b4f3e?w=1920&q=80" -o public/images/hero.jpg`
  - This is a professional woman at a desk — appropriate for a VA agency
  - If this specific URL fails, try an alternative professional portrait: `curl -L "https://images.unsplash.com/photo-1551836022-d5d88e9218df?w=1920&q=80" -o public/images/hero.jpg`
- Verify the file exists and is not empty (file size > 10KB)
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && node -e "const d = require('fs'); const m = require('path'); const f = m.resolve('src/data/homeContent.ts'); const c = d.readFileSync(f,'utf8'); const exports = ['stats','services','valueProps','processSteps','testimonials']; exports.forEach(e => { if(!c.includes('export const ' + e)) throw new Error('Missing export: ' + e); }); console.log('All exports present'); const img = m.resolve('public/images/hero.jpg'); const s = d.statSync(img); if(s.size < 10000) throw new Error('Hero image too small: ' + s.size); console.log('Hero image OK: ' + s.size + ' bytes');"</automated>
  </verify>
  <done>homeContent.ts exports all 5 typed arrays (stats, services, valueProps, processSteps, testimonials) with complete content. Hero photo exists at public/images/hero.jpg and is a real image file (>10KB).</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero, StatsBar, and ServicesGrid components</name>
  <files>src/components/Hero.astro, src/components/StatsBar.astro, src/components/ServicesGrid.astro</files>
  <action>
**Hero.astro** (HOME-01, HOME-07):
- Photo-based hero with navy overlay for text readability
- `<img src="/images/hero.jpg" alt="WorkWiser virtual assistant professional" class="absolute inset-0 w-full h-full object-cover opacity-30" width="1920" height="1080" />`
- Section: `relative min-h-[580px] lg:min-h-[680px] flex items-center overflow-hidden bg-navy`
- Subtitle: "More than a Virtual Assistant agency" — yellow text, uppercase, tracking-widest, font-heading font-bold text-sm
- H1: "We are your Trusted Partners" — white, text-4xl md:text-5xl lg:text-6xl, font-heading font-bold, max-w-2xl
- Body copy: Claude drafts 2-3 sentences about WorkWiser connecting US businesses with skilled nearshore virtual assistants
- Yellow CTA button: "Start delegating" linking to `https://calendly.com/workwiser-info/ceo-client` with `target="_blank" rel="noopener"`
- Button style: `bg-yellow text-navy font-heading font-bold px-7 py-3.5 rounded-md hover:bg-yellow/90 transition-colors`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20`

**StatsBar.astro** (HOME-02):
- Import `stats` from `../data/homeContent.ts`
- Background: `bg-teal py-12`
- Grid: `grid grid-cols-2 lg:grid-cols-4 gap-8 text-center`
- Each stat: render `<span data-count={stat.value} data-suffix={stat.suffix}>{stat.value}{stat.suffix}</span>` — the text content is the FALLBACK value for no-JS
- Label below each number in `text-white/80 text-sm font-medium`
- Number styling: `text-3xl md:text-4xl font-heading font-bold text-white`
- Include `<script>` tag with IntersectionObserver count-up animation:
  - Query `[data-count]` elements with `querySelectorAll<HTMLElement>`
  - On intersection (threshold: 0.3): animate from 0 to target over 1500ms with ease-out cubic
  - Call `observer.unobserve(el)` immediately so it fires only once
  - Cast `entry.target as HTMLElement` for dataset access
  - Use the exact pattern from 03-RESEARCH.md (proven, handles all edge cases)

**ServicesGrid.astro** (HOME-03):
- Import `services` from `../data/homeContent.ts`
- Section heading: "Industries We Serve" or similar — navy, text-3xl, font-heading, centered
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- Each card: rounded-xl, p-6, bg-teal-light with subtle border (border border-teal/20)
- Each card shows: SVG icon (24x24 viewBox, rendered from iconPath via `<path d={service.iconPath} />`), title in font-heading font-bold, description in text-navy/70
- Icon SVG: `<svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-teal" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d={service.iconPath} /></svg>`
- "Explore all Roles" link at bottom of section, linking to `/contact` for now (add a comment: `<!-- TODO: Change to /services when that page exists -->`)
- Link style: text-blue font-heading font-bold with arrow icon, centered

All components use the established container pattern: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npx astro check 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero.astro renders with photo, exact headline text, yellow CTA to Calendly. StatsBar.astro shows 4 stats with IntersectionObserver count-up (fallback shows real numbers without JS). ServicesGrid.astro shows 6 cards with icons. All three import from homeContent.ts. `npm run build` succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0 (no TypeScript or Astro errors)
- homeContent.ts exports all 5 typed arrays
- Hero photo exists at public/images/hero.jpg (not broken)
- StatsBar uses IntersectionObserver (not scroll events) and has fallback text content
- All components import data from homeContent.ts (no hardcoded content in markup)
</verification>

<success_criteria>
1. homeContent.ts is complete with typed data for ALL 6 sections (including Plan 02 sections)
2. Hero shows photo with overlay, exact headline/subtitle text, yellow "Start delegating" CTA to Calendly
3. StatsBar shows 4 stats with count-up animation; shows correct numbers without JS
4. ServicesGrid shows 6 industry cards with icons and "Explore all Roles" link
5. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-01-SUMMARY.md`
</output>
