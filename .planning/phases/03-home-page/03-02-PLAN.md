---
phase: 03-home-page
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/ValueProps.astro
  - src/components/ProcessSteps.astro
  - src/components/Testimonials.astro
  - src/pages/index.astro
autonomous: false
requirements: [HOME-04, HOME-05, HOME-06]

must_haves:
  truths:
    - "Value propositions section shows all 6 items (Simplified Hiring, IRS Compliance, Full Contract Management, Customized Roles, Bilingual Talent, Flexible Payroll Options) with icons and descriptions"
    - "Process section shows 5 numbered steps in order with visual connectors"
    - "Testimonials section shows 3 cards with quote text, name, title, and circular initials avatar"
    - "index.astro composes all 6 sections in conversion-optimized order: Hero, StatsBar, ServicesGrid, ValueProps, ProcessSteps, Testimonials"
    - "Full home page renders correctly at localhost with all sections visible"
  artifacts:
    - path: "src/components/ValueProps.astro"
      provides: "6 value proposition cards"
      min_lines: 15
    - path: "src/components/ProcessSteps.astro"
      provides: "5-step process with visual flow"
      min_lines: 15
    - path: "src/components/Testimonials.astro"
      provides: "3 testimonial cards"
      min_lines: 15
    - path: "src/pages/index.astro"
      provides: "Complete home page composing all 6 sections"
      contains: "Hero"
  key_links:
    - from: "src/components/ValueProps.astro"
      to: "src/data/homeContent.ts"
      via: "import { valueProps }"
      pattern: "import.*valueProps.*from.*homeContent"
    - from: "src/components/ProcessSteps.astro"
      to: "src/data/homeContent.ts"
      via: "import { processSteps }"
      pattern: "import.*processSteps.*from.*homeContent"
    - from: "src/components/Testimonials.astro"
      to: "src/data/homeContent.ts"
      via: "import { testimonials }"
      pattern: "import.*testimonials.*from.*homeContent"
    - from: "src/pages/index.astro"
      to: "All 6 section components"
      via: "import and compose in template"
      pattern: "import.*from.*components"
---

<objective>
Build the remaining three home page sections (ValueProps, ProcessSteps, Testimonials) and compose all six sections into the final index.astro page. After this plan, the home page is complete and ready for visual verification.

Purpose: These below-the-fold sections complete the AIDA conversion flow — Desire (value props + process) and Action (testimonials as final social proof). Composing index.astro wires everything together into a shippable page.

Output: 3 Astro components, updated index.astro with all 6 sections
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-RESEARCH.md
@.planning/phases/03-home-page/03-01-SUMMARY.md

@src/data/homeContent.ts
@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/pages/index.astro

<interfaces>
<!-- From homeContent.ts (created in Plan 01) -->
From src/data/homeContent.ts:
```typescript
export interface ValueProp {
  title: string;
  description: string;
  iconPath: string; // SVG d= attribute string
}
export const valueProps: ValueProp[]; // 6 items

export interface ProcessStep {
  step: number;
  title: string;
  description: string;
}
export const processSteps: ProcessStep[]; // 5 items

export interface Testimonial {
  quote: string;
  name: string;
  title: string;
  initials: string;
}
export const testimonials: Testimonial[]; // 3 items
```

<!-- Components from Plan 01 (import paths) -->
import Hero from '../components/Hero.astro';
import StatsBar from '../components/StatsBar.astro';
import ServicesGrid from '../components/ServicesGrid.astro';

<!-- Brand tokens -->
Colors: bg-navy, text-navy, bg-blue, text-blue, bg-teal, text-teal, bg-teal-light, bg-yellow, text-yellow
Fonts: font-heading (Open Sauce One), font-body (Inter)
Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ValueProps, ProcessSteps, and Testimonials components</name>
  <files>src/components/ValueProps.astro, src/components/ProcessSteps.astro, src/components/Testimonials.astro</files>
  <action>
**ValueProps.astro** (HOME-04):
- Import `valueProps` from `../data/homeContent.ts`
- Section: white or light background (`bg-white py-16 lg:py-24`)
- Heading: "Grow your vision with talented virtual assistants" — verbatim from requirements, navy, text-3xl md:text-4xl, font-heading font-bold, centered
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8`
- Each item: icon (teal colored, rendered from iconPath SVG d attribute in a 24x24 viewBox, same pattern as ServicesGrid), title in font-heading font-bold text-navy, description in text-navy/70
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`

**ProcessSteps.astro** (HOME-05):
- Import `processSteps` from `../data/homeContent.ts`
- Section: light gray or teal-light background (`bg-teal-light py-16 lg:py-24`) for visual contrast with adjacent white sections
- Heading: "How It Works" or "Our Simple Process" — navy, text-3xl md:text-4xl, font-heading font-bold, centered
- Layout: horizontal on desktop (flex or grid), vertical on mobile
  - Desktop: `grid grid-cols-5 gap-4` with connecting lines/arrows between steps
  - Mobile: vertical stack with step numbers
- Each step:
  - Circle with step number: `w-12 h-12 rounded-full bg-teal text-white font-heading font-bold flex items-center justify-center`
  - Title: font-heading font-bold text-navy
  - Description: text-navy/70 text-sm
- Connecting visual between steps (CSS): a horizontal line or dashed border between circles on desktop; a vertical line on mobile. Keep it simple — a `border-t-2 border-dashed border-teal/30` between items works.

**Testimonials.astro** (HOME-06):
- Import `testimonials` from `../data/homeContent.ts`
- Section: white background (`bg-white py-16 lg:py-24`)
- Heading: "Real Results Delivered By WorkWiser VA's" — verbatim from requirements, navy, text-3xl md:text-4xl, font-heading font-bold, centered
- Grid: `grid grid-cols-1 md:grid-cols-3 gap-6`
- Each card (use `<figure>` + `<blockquote>` + `<figcaption>` for semantic markup):
  - Card wrapper: `bg-white rounded-xl shadow-sm border border-navy/10 p-6`
  - Quote text in `<blockquote>`: text-navy/80 text-sm leading-relaxed, wrapped in quotation marks
  - `<figcaption>`: flex row with circular initials avatar and name/title
  - Initials avatar: `w-10 h-10 rounded-full bg-teal flex items-center justify-center text-white font-heading font-bold text-sm`
  - Name: font-heading font-bold text-navy text-sm
  - Title: text-navy/60 text-xs
- Optionally add a bottom CTA: "Ready to get started?" with a yellow "Book a Free Consultation" button linking to Calendly — this provides a final conversion point at the page bottom (Claude's discretion per CONTEXT.md)

All components use `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` container pattern.
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npx astro check 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ValueProps.astro shows 6 items with heading "Grow your vision with talented virtual assistants". ProcessSteps.astro shows 5 numbered steps with visual connectors. Testimonials.astro shows 3 cards with semantic blockquote markup. All import from homeContent.ts. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Compose all sections in index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the placeholder content in `src/pages/index.astro` with imports and composition of all 6 section components.

**Frontmatter:** Import BaseLayout and all 6 components:
```
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import StatsBar from '../components/StatsBar.astro';
import ServicesGrid from '../components/ServicesGrid.astro';
import ValueProps from '../components/ValueProps.astro';
import ProcessSteps from '../components/ProcessSteps.astro';
import Testimonials from '../components/Testimonials.astro';
```

**Template:** Wrap in BaseLayout with updated title and description:
- title: "WorkWiser - Your Trusted Virtual Assistant Partners"
- description: "WorkWiser connects US businesses with skilled nearshore virtual assistants. Simplified hiring, IRS compliance, and bilingual talent for Customer Support, Real Estate, Healthcare, and more."

**Section order (conversion-optimized AIDA model per RESEARCH.md):**
1. Hero (Attention — what we do + CTA)
2. StatsBar (Interest — credibility proof)
3. ServicesGrid (Interest — what roles we fill)
4. ValueProps (Desire — why choose us)
5. ProcessSteps (Desire — how it works)
6. Testimonials (Action — social proof to close)

Each component renders as a self-contained `<section>` — no wrapper divs needed in index.astro. Just place them sequentially inside the BaseLayout slot.
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npm run build 2>&1 | tail -3 && node -e "const fs=require('fs'); const c=fs.readFileSync('src/pages/index.astro','utf8'); const components=['Hero','StatsBar','ServicesGrid','ValueProps','ProcessSteps','Testimonials']; components.forEach(comp => { if(!c.includes(comp)) throw new Error('Missing component: '+comp); }); console.log('All 6 components composed in index.astro');"</automated>
  </verify>
  <done>index.astro imports and renders all 6 section components in AIDA order. `npm run build` succeeds. The complete home page is ready for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete home page</name>
  <files>src/pages/index.astro</files>
  <action>Start the dev server with `npm run dev` and present the verification checklist to the user. The user visually inspects the complete home page at http://localhost:4321.</action>
  <what-built>Complete home page with all 6 sections: Hero (photo + headline + yellow CTA), StatsBar (4 stats with count-up animation), ServicesGrid (6 industry cards), ValueProps (6 differentiators), ProcessSteps (5-step flow), and Testimonials (3 cards with initials avatars)</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project directory
    2. Open http://localhost:4321 in a browser
    3. Verify Hero section:
       - Photo is visible (not broken image icon)
       - Subtitle reads "More than a Virtual Assistant agency"
       - H1 reads "We are your Trusted Partners"
       - Yellow "Start delegating" button is visible and links to Calendly
    4. Scroll down to Stats bar:
       - Numbers animate up from 0 (count-up effect)
       - Final values: 300+, 100+, 150, 10+
       - Background is teal with white text
    5. Verify Services grid shows 6 cards with icons
    6. Verify Value Props shows 6 items with heading "Grow your vision with talented virtual assistants"
    7. Verify Process steps shows 5 numbered steps with visual flow
    8. Verify Testimonials shows 3 cards with quotes and initials avatars
    9. Resize browser to mobile width — verify all sections stack properly
    10. Test count-up on fresh page load where stats are already in viewport (resize browser to be very short, reload)
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually approved the complete home page at localhost:4321. All 6 sections render correctly on desktop and mobile.</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- All 6 components are imported and rendered in index.astro
- Each component imports its data from homeContent.ts (no hardcoded content)
- Section order follows AIDA: Hero > Stats > Services > ValueProps > Process > Testimonials
- Page is responsive on mobile, tablet, and desktop
- Count-up animation works when scrolling to stats AND when stats are already in viewport on load
</verification>

<success_criteria>
1. ValueProps shows all 6 differentiators with the exact heading from requirements
2. ProcessSteps shows all 5 steps in numbered order with visual connectors
3. Testimonials shows 3 placeholder cards with semantic blockquote markup
4. index.astro composes all 6 sections in AIDA conversion order
5. Full home page renders correctly at localhost:4321
6. Visual verification approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-02-SUMMARY.md`
</output>
