---
phase: 04-about-us-why-us-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/aboutContent.ts
  - src/data/whyUsContent.ts
  - src/components/AboutHero.astro
  - src/components/VisionMission.astro
  - src/components/CoreValues.astro
  - src/components/OurStory.astro
  - src/components/TeamGrid.astro
  - src/components/StrategicLocation.astro
  - src/pages/about.astro
  - public/images/team/placeholder-1.jpg
  - public/images/team/placeholder-2.jpg
  - public/images/team/placeholder-3.jpg
  - public/images/team/placeholder-4.jpg
  - public/images/about-values.jpg
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05
  - ABOUT-06

must_haves:
  truths:
    - "About Us page loads at /about with hero, mission, vision, values, story, team, and location sections"
    - "Hero shows teal-to-blue gradient with centered white text and yellow Start today CTA linking to Calendly"
    - "Vision and Mission sections display correct copy in light teal card layout"
    - "Core values section shows 4-6 values with icons arranged around a center photo on teal gradient background"
    - "Our Story section shows polished company history with gradient text heading"
    - "Team section renders placeholder grid with downloadable stock photos"
    - "Strategic Location section shows dot-grid Americas SVG map on blue gradient background"
  artifacts:
    - path: "src/data/aboutContent.ts"
      provides: "All About Us page content — vision, mission, values, story, team placeholders"
      exports: ["vision", "mission", "coreValues", "ourStory", "teamMembers"]
    - path: "src/data/whyUsContent.ts"
      provides: "All Why Us page content — differentiators, recruitment steps, FAQs"
      exports: ["differentiators", "recruitmentSteps", "faqs"]
    - path: "src/components/StrategicLocation.astro"
      provides: "Shared dot-grid Americas map section used on both pages"
    - path: "src/pages/about.astro"
      provides: "About Us page route at /about"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/components/AboutHero.astro"
      via: "Astro component import"
      pattern: "import AboutHero from"
    - from: "src/components/CoreValues.astro"
      to: "src/data/aboutContent.ts"
      via: "data import"
      pattern: "import.*coreValues.*from.*aboutContent"
    - from: "src/pages/about.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "import BaseLayout from"
---

<objective>
Build the About Us page with all six sections (Hero, Vision/Mission, Core Values, Our Story, Team, Strategic Location) and both data files (aboutContent.ts and whyUsContent.ts). Also create the shared StrategicLocation component used by both pages.

Purpose: Visitors who want to learn about WorkWiser's story, mission, and team can access a complete, brand-accurate About Us page matching the Canva mockup design.
Output: Working /about page with all content sections, both data files, and the shared StrategicLocation component ready for Why Us page reuse.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-us-why-us-pages/04-CONTEXT.md
@.planning/phases/04-about-us-why-us-pages/04-RESEARCH.md

@src/data/homeContent.ts
@src/pages/index.astro
@src/components/CtaBanner.astro
@src/components/Hero.astro
@src/styles/global.css

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase. -->

From src/data/homeContent.ts (PATTERN TO FOLLOW):
- Export interfaces with typed fields
- Export const arrays/objects with content
- SVG icon paths as `d` attribute strings in 24x24 viewBox

From src/pages/index.astro (PAGE COMPOSITION PATTERN):
- Import BaseLayout from '../layouts/BaseLayout.astro'
- Import section components
- Wrap in <BaseLayout title="..." description="...">
- Stack section components in order

From src/components/CtaBanner.astro (W-ARROW SVG PATHS — copy verbatim):
- W-arrow SVG viewBox="0 0 556.27 519.13"
- Two path elements with exact `d` attribute data
- Used as watermark with opacity and pointer-events-none

Brand tokens from src/styles/global.css:
--color-navy: #003c64
--color-blue: #0072c9
--color-teal: #26aeb4
--color-teal-light: #effefe
--color-yellow: #f3f145
Font classes: font-heading (Open Sauce One), font-body (Inter)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data files and download stock photos</name>
  <files>src/data/aboutContent.ts, src/data/whyUsContent.ts, public/images/team/, public/images/about-values.jpg</files>
  <action>
Create `src/data/aboutContent.ts` following the homeContent.ts pattern with these exports:

1. **vision** object: `{ heading: "Our Vision", text: "To connect exceptional talent with extraordinary opportunities, empowering individuals and organizations to achieve their full potential." }`

2. **mission** object: `{ heading: "Our Mission", text: "To be the preferred recruitment partner, known for our outstanding service, unwavering commitment, and transformative solutions that create opportunities for businesses and talented professionals alike." }` — Polish grammar, bold key phrases marked with a `boldPhrases: string[]` field.

3. **coreValues** array (select best 4-6 from combined sets: Integrity, Collaboration, Excellence, Innovation, Passion, Drive to Growth, Exceptional Attitude, Dedication). Each value has: `title`, `description` (2-3 sentences), `iconPath` (SVG d attribute, 24x24 viewBox, Heroicons outline style). Claude selects the final set per user's discretion allowance.

4. **ourStory** object: `{ heading: "Our Story", paragraphs: string[] }` — Three paragraphs of company history. Key facts to preserve: launched September 2020, started with one VA, grown to 150+ team members, win-win environment. Polish the Canva text for grammar and professional flow while preserving bold key phrases.

5. **teamMembers** array: 4 placeholder entries with `{ name, title, imageUrl }`. Use paths like `/images/team/placeholder-1.jpg`.

6. **strategicLocation** object: `{ heading, description }` — Text about Central American agents covering North American time zones.

Create `src/data/whyUsContent.ts` with these exports:

1. **differentiators** array: 6 items (Tailored Approach, Extensive Expertise, Thorough Screening Process, Confidentiality and Privacy, Continuous Support, Time and Cost Efficiency). Each has: `title`, `description` (professional paragraph written by Claude), `iconPath` (SVG d attribute, 24x24 viewBox).

2. **recruitmentSteps** array: 8 strings matching the Canva bullet points: Client consultation, Job analysis and position profiling, Candidate sourcing, Screening and presentation, Skill assessment and testing, Candidate presentation, Client interviews, Client management.

3. **faqs** array: Claude writes 6-8 WorkWiser-specific FAQ Q&As. Replace ALL Virtual Latinos references. Questions should cover: What is a VA? How does WorkWiser screen candidates? What industries do you serve? How quickly can I get started? What time zones do your VAs work? How does billing work? Can I scale my team up/down? What if I'm not satisfied with my VA?

Download stock photos to local `public/images/` directory:
- 4 professional headshot photos for team placeholders → `public/images/team/placeholder-1.jpg` through `placeholder-4.jpg`
- 1 photo of two professionals collaborating/high-fiving → `public/images/about-values.jpg`

Use `curl` to download from Unsplash (same approach as existing hero.jpg). Do NOT hotlink — download to local files per Pitfall 5 in RESEARCH.md.
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && node -e "const a = require('./src/data/aboutContent.ts'); console.log('FAIL: should not be CommonJS')" 2>&1 || npx astro check 2>&1 | head -5; test -f public/images/team/placeholder-1.jpg && echo "photos downloaded" || echo "MISSING photos"</automated>
  </verify>
  <done>Both data files export typed content matching the Canva mockup copy. All 5 stock photos exist locally in public/images/. No TypeScript errors when imported by Astro.</done>
</task>

<task type="auto">
  <name>Task 2: Build About Us section components and compose about.astro page</name>
  <files>src/components/AboutHero.astro, src/components/VisionMission.astro, src/components/CoreValues.astro, src/components/OurStory.astro, src/components/TeamGrid.astro, src/components/StrategicLocation.astro, src/pages/about.astro</files>
  <action>
Build 6 Astro components and compose the page. All components import data from aboutContent.ts.

**AboutHero.astro** — ABOUT-01:
- Full-width section with `bg-gradient-to-br from-teal via-blue to-navy` (diagonal gradient per Pitfall 1)
- W-arrow watermark: copy SVG path data verbatim from CtaBanner.astro, use `text-white opacity-10 pointer-events-none` positioned absolute
- Centered `<h1>` in white: "We are passionate about bringing together the right talent and the right opportunities to create successful partnerships!"
- Yellow "Start today" CTA button linking to `https://calendly.com/workwiser-info/ceo-client` with `target="_blank" rel="noopener"` — use same button classes as Hero.astro CTA
- Generous padding: `py-24 md:py-32`

**VisionMission.astro** — ABOUT-02, ABOUT-03, ABOUT-04:
- Light teal background (`bg-teal-light`)
- Two rows, each with: heading on left ("Our Vision" / "Our Mission"), description in a light blue/gray rounded card on right
- Layout: `md:grid md:grid-cols-2 gap-8` for each row
- Card styling: `bg-white rounded-2xl p-8 shadow-sm`
- Bold key phrases within the description text — use `<strong>` tags
- Import vision and mission from aboutContent.ts

**CoreValues.astro** — ABOUT-05:
- Teal gradient background: `bg-gradient-to-br from-teal via-teal to-blue`
- W-arrow watermark (copy from CtaBanner, use `text-white opacity-10`)
- CSS Grid approach for layout: 3-column grid where the center cell holds the collaborating photo (`about-values.jpg`), and value items are positioned in the surrounding cells
- Each value: circular navy icon container (`w-16 h-16 rounded-full bg-navy flex items-center justify-center`) + title + description
- Responsive: on mobile, stack values vertically with photo between them
- Import coreValues from aboutContent.ts

**OurStory.astro**:
- Light background (white or `bg-gray-50`)
- Centered heading "Our Story" with gradient text: `bg-gradient-to-r from-teal to-navy bg-clip-text text-transparent` (MUST pair bg-clip-text AND text-transparent per Pitfall 2)
- Three paragraphs of polished company history from aboutContent.ts
- Max-width container for readable line lengths: `max-w-3xl mx-auto`

**TeamGrid.astro** — ABOUT-06:
- Light background
- Heading "Meet Our Team" or similar
- CSS grid: `grid grid-cols-2 md:grid-cols-4 gap-6`
- Each card: photo (from local `/images/team/` files), name, title
- Photo styling: `rounded-xl object-cover aspect-square`
- Import teamMembers from aboutContent.ts

**StrategicLocation.astro** (SHARED component — used by both pages):
- Blue gradient background: `bg-gradient-to-br from-navy via-navy to-blue`
- Split layout: text on left, dot-grid Americas SVG on right
- Left side: heading "Strategic Location" + description about Central American agents in North American time zones
- Right side: Inline SVG with `viewBox="0 0 480 400"`, grid of `<circle>` elements with `r="3"` forming a recognizable outline of North and Central America using dots on ~12px spacing. Use `text-teal` or `text-teal-light` for dot color. Set `width="100%" class="max-w-lg"` for responsive scaling (Pitfall 6). Mark `aria-hidden="true"`.
- Claude authors the SVG dot coordinates during implementation — aim for ~150-200 dots tracing recognizable landmass outlines
- Import strategicLocation from aboutContent.ts

**about.astro** page:
- Follow index.astro composition pattern exactly
- Import BaseLayout + all 6 section components
- `<BaseLayout title="About Us - WorkWiser" description="Learn about WorkWiser's mission, vision, core values, and the team dedicated to connecting businesses with exceptional talent.">`
- Stack components in order: AboutHero, VisionMission, CoreValues, OurStory, TeamGrid, StrategicLocation
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>About Us page builds with zero errors. Navigating to /about shows all 6 sections in correct order: gradient hero with CTA, vision/mission cards, core values with photo, our story with gradient heading, team placeholder grid, and strategic location with dot-grid map.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `npm run dev` — navigate to `http://localhost:4321/about` — all 6 sections render in correct order
3. Hero gradient is diagonal (teal top-left to navy bottom-right), not horizontal
4. Yellow CTA links to Calendly and opens in new tab
5. Vision/Mission cards show correct copy with bold key phrases
6. Core values shows 4-6 values with icons arranged around center photo
7. Our Story heading shows gradient text (not invisible — Pitfall 2)
8. Team photos load from local files (not broken — Pitfall 5)
9. Dot-grid map scales correctly on mobile (not overflowing — Pitfall 6)
10. StrategicLocation component is importable from `../components/StrategicLocation.astro` for reuse in Why Us plan
</verification>

<success_criteria>
- /about page loads and renders all sections matching the Canva mockup layout
- Both aboutContent.ts and whyUsContent.ts export correctly typed data
- StrategicLocation.astro is a standalone shared component ready for Why Us page reuse
- All stock photos are local files (no hotlinking)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-us-why-us-pages/04-01-SUMMARY.md`
</output>
