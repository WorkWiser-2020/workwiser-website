---
phase: 04-about-us-why-us-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/WhyUsHero.astro
  - src/components/Differentiators.astro
  - src/components/RecruitmentProcess.astro
  - src/components/FaqAccordion.astro
  - src/pages/why-us.astro
  - public/images/recruitment-professional.jpg
autonomous: true
requirements:
  - WHY-01
  - WHY-02
  - WHY-03

must_haves:
  truths:
    - "Why Us page loads at /why-us with hero, differentiators, recruitment process, FAQ, and strategic location sections"
    - "Hero shows WorkWiser is your best option with 'your best' in yellow text and Start today CTA"
    - "Six differentiators display in a 3x2 grid with navy icon containers and professional descriptions"
    - "Recruitment process section shows 8 bullet points with split layout and stock photo"
    - "FAQ accordion opens and closes with accessible aria-expanded toggle"
    - "Strategic Location shared component renders identically to About Us page"
    - "All CTAs link to Calendly"
  artifacts:
    - path: "src/pages/why-us.astro"
      provides: "Why Us page route at /why-us"
    - path: "src/components/WhyUsHero.astro"
      provides: "Centered gradient hero with yellow text highlight"
    - path: "src/components/Differentiators.astro"
      provides: "3x2 grid of differentiator cards with icons"
    - path: "src/components/FaqAccordion.astro"
      provides: "Accessible accordion with vanilla JS toggle"
  key_links:
    - from: "src/pages/why-us.astro"
      to: "src/components/StrategicLocation.astro"
      via: "shared component import from Plan 01"
      pattern: "import StrategicLocation from"
    - from: "src/components/Differentiators.astro"
      to: "src/data/whyUsContent.ts"
      via: "data import"
      pattern: "import.*differentiators.*from.*whyUsContent"
    - from: "src/components/FaqAccordion.astro"
      to: "src/data/whyUsContent.ts"
      via: "data import for FAQ items"
      pattern: "import.*faqs.*from.*whyUsContent"
---

<objective>
Build the Why Us page with all five sections (Hero, Differentiators, Recruitment Process, FAQ Accordion, Strategic Location) using the whyUsContent.ts data file created in Plan 01.

Purpose: Prospects evaluating WorkWiser can see clear differentiators, understand the recruitment process, get answers to common questions, and take action via the CTA — all on a brand-accurate page matching the Canva mockup.
Output: Working /why-us page with all content sections, interactive FAQ accordion, and shared StrategicLocation component reuse.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-us-why-us-pages/04-CONTEXT.md
@.planning/phases/04-about-us-why-us-pages/04-RESEARCH.md
@.planning/phases/04-about-us-why-us-pages/04-01-SUMMARY.md

@src/data/whyUsContent.ts
@src/components/StrategicLocation.astro
@src/components/CtaBanner.astro
@src/components/Hero.astro
@src/components/Header.astro
@src/pages/about.astro
@src/styles/global.css

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From src/data/whyUsContent.ts (created in Plan 01):
```typescript
export interface Differentiator {
  title: string;
  description: string;
  iconPath: string;
}
export const differentiators: Differentiator[];

export interface RecruitmentStep { /* or string[] */ }
export const recruitmentSteps: string[];

export interface Faq {
  question: string;
  answer: string;
}
export const faqs: Faq[];
```

From src/components/StrategicLocation.astro (created in Plan 01):
- Standalone component, no props needed
- Import and use: `<StrategicLocation />`

From src/components/Header.astro (VANILLA JS PATTERN for FAQ):
- Uses `<script>` tag at bottom of component
- querySelectorAll for toggle buttons
- aria-expanded attribute on <button> elements
- classList.toggle for showing/hiding content

Brand tokens:
--color-navy: #003c64, --color-teal: #26aeb4, --color-yellow: #f3f145
Gradient: bg-gradient-to-br from-teal via-blue to-navy
Yellow CTA: bg-yellow text-navy font-heading font-bold px-7 py-3.5 rounded-full
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Why Us section components and download stock photo</name>
  <files>src/components/WhyUsHero.astro, src/components/Differentiators.astro, src/components/RecruitmentProcess.astro, src/components/FaqAccordion.astro, public/images/recruitment-professional.jpg</files>
  <action>
Build 4 Astro components. All import data from whyUsContent.ts (created in Plan 01).

**WhyUsHero.astro** — WHY-01, WHY-03:
- Full-width section with `bg-gradient-to-br from-teal via-blue to-navy` (same diagonal gradient as AboutHero)
- W-arrow watermark: copy SVG paths verbatim from CtaBanner.astro, `text-white opacity-10 pointer-events-none` positioned absolute
- Centered `<h1>` in white: "WorkWiser is " + `<span class="text-yellow">your best</span>` + " option" — the "your best" text in yellow per CONTEXT.md
- Yellow "Start today" CTA button: `https://calendly.com/workwiser-info/ceo-client`, `target="_blank" rel="noopener"`, same button classes as all other yellow CTAs
- Padding: `py-24 md:py-32`

**Differentiators.astro** — WHY-02:
- Light background (white or `bg-gray-50`)
- Section heading: "Why Choose WorkWiser" or similar, centered
- 3x2 responsive grid: `grid md:grid-cols-2 lg:grid-cols-3 gap-8`
- Each card: navy rounded-square icon container (`w-14 h-14 rounded-xl bg-navy flex items-center justify-center flex-shrink-0`) with white SVG icon, title in `font-heading font-bold`, paragraph description
- Import differentiators array from whyUsContent.ts
- Map over array to render cards

**RecruitmentProcess.astro**:
- Light background with dot-grid pattern (subtle teal dots as background, CSS or inline SVG — matches Canva's dot pattern treatment)
- Split layout: `md:grid md:grid-cols-2 gap-12 items-center`
- Left side: Heading "WorkWiser's top-notch recruitment process" (gradient text: `bg-gradient-to-r from-teal to-navy bg-clip-text text-transparent`) + ordered list of 8 bullet points from recruitmentSteps array
- Right side: Stock photo of professional with headset. Download a suitable Unsplash photo to `public/images/recruitment-professional.jpg` (do NOT hotlink). Style the photo with a blue shape background behind it — use a `relative` container with a `bg-blue rounded-2xl` div positioned absolute behind the image, offset slightly.
- Bullet points: Claude decides whether to use simple numbered dots or small icons (per discretion). Simple numbered circles recommended for clarity.
- Import recruitmentSteps from whyUsContent.ts

**FaqAccordion.astro**:
- Light background
- Split layout on desktop: heading on left ("Frequently asked questions" in gradient text), accordion on right
- `md:grid md:grid-cols-[1fr_2fr] gap-12`
- Accordion items: each has a `<button>` with `aria-expanded="false"` and `data-index`, the question text, and a chevron SVG that rotates 180 degrees on open
- Answer `<div>` starts with `hidden` class (Tailwind's display:none — instant toggle, no animation, per Pitfall 3 and RESEARCH recommendation)
- `<script>` tag at bottom with vanilla JS: `querySelectorAll('.faq-trigger')` → click handler toggles `aria-expanded`, toggles `hidden` on next sibling, toggles `rotate-180` on chevron icon
- Use `.faq-trigger` class on buttons for targeted selection (same pattern as Header.astro's `.mobile-nav-link`)
- Border between items: `border-b border-navy/10`
- Import faqs from whyUsContent.ts
- First FAQ item optionally starts open (Claude's discretion)

Download stock photo:
- Professional with headset → `public/images/recruitment-professional.jpg` via curl from Unsplash
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && test -f public/images/recruitment-professional.jpg && echo "photo exists" || echo "MISSING photo"; test -f src/components/FaqAccordion.astro && grep -c "aria-expanded" src/components/FaqAccordion.astro</automated>
  </verify>
  <done>All 4 Why Us section components exist with correct data imports. FaqAccordion has aria-expanded accessibility. Stock photo downloaded locally. No TypeScript errors in component files.</done>
</task>

<task type="auto">
  <name>Task 2: Compose why-us.astro page and verify full build</name>
  <files>src/pages/why-us.astro</files>
  <action>
Create `src/pages/why-us.astro` following the exact same composition pattern as index.astro and about.astro:

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import WhyUsHero from '../components/WhyUsHero.astro';
import Differentiators from '../components/Differentiators.astro';
import RecruitmentProcess from '../components/RecruitmentProcess.astro';
import FaqAccordion from '../components/FaqAccordion.astro';
import StrategicLocation from '../components/StrategicLocation.astro';
---
<BaseLayout
  title="Why Us - WorkWiser"
  description="Discover why WorkWiser is your best option for nearshore virtual assistants. Tailored approach, thorough screening, continuous support, and cost efficiency."
>
  <WhyUsHero />
  <Differentiators />
  <RecruitmentProcess />
  <FaqAccordion />
  <StrategicLocation />
</BaseLayout>
```

After creating the page, run `npm run build` to verify zero errors across the entire site (all 3 pages: index, about, why-us).

Then run `npm run dev` briefly to confirm the page renders. Verify:
- All 5 sections appear in correct order
- FAQ accordion JS works (click to expand/collapse)
- StrategicLocation component renders the same as on /about
- All images load from local files
- Yellow CTAs link to Calendly
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Why Us page builds with zero errors. Navigating to /why-us shows all 5 sections: gradient hero with yellow "your best" text, 3x2 differentiators grid, recruitment process with photo, working FAQ accordion, and strategic location map. Full site builds clean (index + about + why-us).</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors for all pages
2. `npm run dev` — navigate to `http://localhost:4321/why-us` — all 5 sections render
3. Hero shows "your best" in yellow text within white heading
4. Six differentiators display in responsive grid with navy icon containers
5. Recruitment process shows 8 numbered steps with stock photo
6. FAQ accordion: clicking a question expands the answer, clicking again collapses it
7. FAQ accordion: `aria-expanded` toggles correctly for screen readers
8. StrategicLocation section matches /about page exactly (shared component)
9. All CTAs link to `calendly.com/workwiser-info/ceo-client` with `rel="noopener"`
10. Stock photo loads from local file (not broken)
</verification>

<success_criteria>
- /why-us page loads and renders all sections matching the Canva mockup layout
- FAQ accordion is interactive and accessible (aria-expanded)
- StrategicLocation component is reused from Plan 01 (not duplicated)
- All images are local files
- Full site build (index + about + why-us) passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-us-why-us-pages/04-02-SUMMARY.md`
</output>
