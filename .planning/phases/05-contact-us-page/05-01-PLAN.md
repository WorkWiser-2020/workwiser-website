---
phase: 05-contact-us-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/contactContent.ts
  - src/layouts/BaseLayout.astro
  - src/components/ContactHero.astro
  - src/components/ContactSidebar.astro
autonomous: true
requirements:
  - CONT-01
  - CONT-05
  - CONT-06
  - CONT-07

must_haves:
  truths:
    - "Contact page hero renders with 'Let's meet!' headline, subtitle, and yellow Calendly CTA"
    - "Stats sidebar shows 4 stats (100+ Clients, 300+ Projects, 10+ Years, 150 Team) with count-up animation"
    - "Contact info displays phone (302) 257-5427 and email info@workwiser.io in sidebar"
    - "Calendly popup opens on 'Book a Free Consultation' button click"
  artifacts:
    - path: "src/data/contactContent.ts"
      provides: "Stats, contact info, Calendly URL, form field metadata"
      exports: ["contactStats", "contactInfo", "formFields"]
    - path: "src/components/ContactHero.astro"
      provides: "Hero section matching teal-to-navy gradient pattern"
      min_lines: 20
    - path: "src/components/ContactSidebar.astro"
      provides: "Vertical stats + contact info + Calendly booking card"
      min_lines: 40
    - path: "src/layouts/BaseLayout.astro"
      provides: "Head slot for page-specific script/style injection"
      contains: "slot name=\"head\""
  key_links:
    - from: "src/components/ContactSidebar.astro"
      to: "src/data/contactContent.ts"
      via: "import contactStats, contactInfo"
      pattern: "import.*from.*contactContent"
    - from: "src/components/ContactHero.astro"
      to: "WhyUsHero.astro pattern"
      via: "same gradient + W-arrow SVG"
      pattern: "bg-gradient-to-br.*from-teal.*via-blue.*to-navy"
    - from: "src/components/ContactSidebar.astro"
      to: "Calendly popup widget"
      via: "Calendly.initPopupWidget() onclick"
      pattern: "Calendly\\.initPopupWidget"
---

<objective>
Build the Contact Us page data layer, hero, and sidebar components, plus add a head slot to BaseLayout for Calendly widget loading.

Purpose: Establishes all non-form components of the contact page and the data file that both this plan and Plan 02 (form) will consume. The BaseLayout head slot is a prerequisite for loading Calendly's popup widget JS only on the contact page.

Output: contactContent.ts, ContactHero.astro, ContactSidebar.astro, updated BaseLayout.astro with head slot
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contact-us-page/05-CONTEXT.md
@.planning/phases/05-contact-us-page/05-RESEARCH.md

@src/components/WhyUsHero.astro
@src/components/StatsBar.astro
@src/data/homeContent.ts
@src/layouts/BaseLayout.astro

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/homeContent.ts (pattern to follow):
```typescript
export interface Stat {
  value: number;
  suffix: string;
  label: string;
}
export const stats: Stat[] = [
  { value: 300, suffix: '+', label: 'Completed Projects' },
  ...
];
```

From src/layouts/BaseLayout.astro (current state — NO head slot):
```astro
interface Props {
  title: string;
  description?: string;
}
```
Head element currently has no `<slot name="head" />`.

From src/components/WhyUsHero.astro (hero pattern to clone):
- Uses `bg-gradient-to-br from-teal via-blue to-navy py-24 md:py-32`
- W-arrow SVG watermark at `opacity-10`
- Yellow CTA button with `rounded-full` and `hover:bg-yellow/90`
- `target="_blank" rel="noopener"` on external links
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data file + BaseLayout head slot</name>
  <files>src/data/contactContent.ts, src/layouts/BaseLayout.astro</files>
  <action>
1. Create `src/data/contactContent.ts` following the established data layer pattern from `homeContent.ts`:
   - Export `ContactStat` interface (value: number, suffix: string, label: string)
   - Export `contactStats` array: 100+ Satisfied Clients, 300+ Projects Completed, 10+ Years Experience, 150 Team Members
   - Export `contactInfo` object: phone "(302) 257-5427", email "info@workwiser.io", calendlyUrl "https://calendly.com/workwiser-info/ceo-client"
   - Export `formFields` array with metadata for all 7 fields (id, label, type, required, half). Required fields: email and location only. Message field uses type "textarea" and half: false. All others: type "text", required: false, half: true.

2. Add `<slot name="head" />` to `BaseLayout.astro` inside the `<head>` element, just before the closing `</head>` tag. This allows page-specific scripts (like Calendly widget.js) to be injected without polluting all pages. Do NOT change anything else in BaseLayout.
  </action>
  <verify>
    <automated>cd "C:\Users\david\OneDrive\Documents\Projects\Build WorkWiser Website" && npx astro check 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>contactContent.ts exports contactStats (4 items), contactInfo (phone + email + calendlyUrl), and formFields (7 items). BaseLayout.astro has a named "head" slot inside the head element. Build succeeds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: ContactHero + ContactSidebar components</name>
  <files>src/components/ContactHero.astro, src/components/ContactSidebar.astro</files>
  <action>
1. Create `src/components/ContactHero.astro` (CONT-01):
   - Clone the WhyUsHero.astro pattern exactly: full-width section with `bg-gradient-to-br from-teal via-blue to-navy py-24 md:py-32`
   - Include the W-arrow SVG watermark (copy the exact SVG paths from WhyUsHero.astro) at `opacity-10`
   - H1: "Let's meet!" — no yellow text span needed (different from Why Us hero)
   - Subtitle paragraph: "Drop a line or give us a call" in `text-white/80`
   - Yellow CTA button: "Book a Free Consultation" linking to `https://calendly.com/workwiser-info/ceo-client` with `target="_blank" rel="noopener"`
   - Button classes: `inline-block bg-yellow text-navy font-heading font-bold px-7 py-3.5 rounded-full hover:bg-yellow/90 transition-colors` (matches WhyUsHero button style)

2. Create `src/components/ContactSidebar.astro` (CONT-05, CONT-06, CONT-07):
   - Import `contactStats` and `contactInfo` from `../data/contactContent.ts`
   - Three sections stacked vertically:

   **Section A — Stats (CONT-05):**
   - Vertical stack of 4 stats using `space-y-6`
   - Each stat: large teal number with `data-count` and `data-suffix` attributes (for count-up animation), label text below
   - Copy the IntersectionObserver count-up `<script>` block verbatim from StatsBar.astro — same `data-count`/`data-suffix` selectors, same easing, same 1500ms duration
   - Use `text-3xl font-heading font-bold text-teal` for numbers, `text-navy/70 text-sm font-medium` for labels

   **Section B — Contact Info (CONT-07):**
   - Phone number: `(302) 257-5427` with a phone SVG icon, wrapped in `<a href="tel:+13022575427">`
   - Email: `info@workwiser.io` with an email SVG icon, wrapped in `<a href="mailto:info@workwiser.io">`
   - Use simple inline SVG icons (24x24 viewBox, stroke style) for phone and email

   **Section C — Calendly Booking Card (CONT-06):**
   - Compact card with background (white or light teal/light blue — Claude's discretion)
   - Heading: "Book a Free Consultation"
   - Brief text: "Schedule a 30-minute call with our team"
   - Button: `onclick="Calendly.initPopupWidget({ url: 'https://calendly.com/workwiser-info/ceo-client' }); return false;"` with yellow styling matching hero CTA
   - `type="button"` to prevent form submission if nested

   Style the overall sidebar with appropriate padding and visual separation between sections (dividers or spacing). Use a white or very light background card style that works against a light page background.
  </action>
  <verify>
    <automated>cd "C:\Users\david\OneDrive\Documents\Projects\Build WorkWiser Website" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>ContactHero.astro renders hero with correct headline, subtitle, W-arrow watermark, and yellow Calendly CTA. ContactSidebar.astro renders vertical stats with count-up animation attributes, phone + email contact info with clickable links, and a Calendly popup booking card with initPopupWidget onclick. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `contactContent.ts` exports 3 named exports: contactStats, contactInfo, formFields
- `BaseLayout.astro` contains `<slot name="head" />`
- `ContactHero.astro` uses teal-to-navy gradient and W-arrow SVG
- `ContactSidebar.astro` has `data-count` attributes, IntersectionObserver script, phone/email links, and Calendly.initPopupWidget
</verification>

<success_criteria>
All four files exist and build passes. Data layer provides typed content for both sidebar and form (consumed by Plan 02). BaseLayout head slot enables page-specific script injection. Hero and sidebar components are ready to compose into contact.astro in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-us-page/05-01-SUMMARY.md`
</output>
