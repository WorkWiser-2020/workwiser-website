---
phase: 05-contact-us-page
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/ContactForm.astro
  - src/pages/contact.astro
  - .planning/phases/05-contact-us-page/ZAPIER-SETUP-GUIDE.md
autonomous: false
requirements:
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Contact form renders all 7 fields (First Name, Last Name, Job Title, Company Name, Email, Location, Message)"
    - "Submitting with empty Email or Location shows inline error messages below the invalid fields"
    - "Successful form submission fades out the form and shows 'Thanks! We will be in touch within 24 hours.' inline"
    - "Form uses Netlify Forms (data-netlify='true') with honeypot spam protection"
    - "Contact page composes hero, form, and sidebar in a responsive two-column layout"
    - "Calendly popup widget JS and CSS load only on the contact page via head slot"
    - "Zapier setup guide documents step-by-step how to connect Netlify Forms to Google Sheets"
  artifacts:
    - path: "src/components/ContactForm.astro"
      provides: "Netlify Forms AJAX form with validation and inline success message"
      min_lines: 80
      contains: "data-netlify"
    - path: "src/pages/contact.astro"
      provides: "Composed contact page with hero, two-column form+sidebar layout"
      min_lines: 20
      contains: "ContactHero"
    - path: ".planning/phases/05-contact-us-page/ZAPIER-SETUP-GUIDE.md"
      provides: "Step-by-step Zapier + Google Sheets integration instructions"
      min_lines: 20
  key_links:
    - from: "src/components/ContactForm.astro"
      to: "Netlify Forms"
      via: "AJAX fetch with URL-encoded body"
      pattern: "application/x-www-form-urlencoded"
    - from: "src/components/ContactForm.astro"
      to: "src/data/contactContent.ts"
      via: "import formFields for dynamic field rendering"
      pattern: "import.*formFields.*from.*contactContent"
    - from: "src/pages/contact.astro"
      to: "src/components/ContactHero.astro"
      via: "component import"
      pattern: "import ContactHero"
    - from: "src/pages/contact.astro"
      to: "src/components/ContactForm.astro"
      via: "component import"
      pattern: "import ContactForm"
    - from: "src/pages/contact.astro"
      to: "src/components/ContactSidebar.astro"
      via: "component import"
      pattern: "import ContactSidebar"
    - from: "src/pages/contact.astro"
      to: "Calendly CDN"
      via: "Fragment slot='head' with widget.js and widget.css"
      pattern: "calendly.com/assets/external/widget"
---

<objective>
Build the contact form component with Netlify Forms AJAX submission, compose the full contact.astro page, and create the Zapier setup guide.

Purpose: Completes the Contact Us page — the primary conversion path for prospective clients. The form captures leads via Netlify Forms (email to info@workwiser.io), the page layout combines hero + form + sidebar, and the Zapier guide enables Google Sheets logging without code.

Output: ContactForm.astro, contact.astro page, ZAPIER-SETUP-GUIDE.md
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contact-us-page/05-CONTEXT.md
@.planning/phases/05-contact-us-page/05-RESEARCH.md
@.planning/phases/05-contact-us-page/05-01-SUMMARY.md

@src/data/contactContent.ts
@src/components/ContactHero.astro
@src/components/ContactSidebar.astro
@src/layouts/BaseLayout.astro
@src/pages/about.astro
@src/pages/why-us.astro

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/data/contactContent.ts:
```typescript
export interface ContactStat {
  value: number;
  suffix: string;
  label: string;
}
export const contactStats: ContactStat[];
export const contactInfo: {
  phone: string;
  email: string;
  calendlyUrl: string;
};
export const formFields: Array<{
  id: string;
  label: string;
  type: string;    // 'text' | 'email' | 'textarea'
  required: boolean;
  half: boolean;   // true = half-width in grid, false = full-width
}>;
```

From src/layouts/BaseLayout.astro (updated in Plan 01):
```astro
<!-- Head now includes a named slot for page-specific assets -->
<head>
  ...
  <slot name="head" />
</head>
```

From src/components/ContactHero.astro (created in Plan 01):
- No props — self-contained hero component

From src/components/ContactSidebar.astro (created in Plan 01):
- No props — imports data from contactContent.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactForm component + contact.astro page + Zapier guide</name>
  <files>src/components/ContactForm.astro, src/pages/contact.astro, .planning/phases/05-contact-us-page/ZAPIER-SETUP-GUIDE.md</files>
  <action>
1. Create `src/components/ContactForm.astro` (CONT-02, CONT-03):
   - Import `formFields` from `../data/contactContent.ts`
   - Render a `<form>` element with these exact attributes: `id="contact-form"`, `name="contact"`, `method="POST"`, `data-netlify="true"`, `netlify-honeypot="bot-field"`, `novalidate`
   - Include hidden fields: `<input type="hidden" name="form-name" value="contact" />` (required for AJAX submission) and honeypot `<p class="hidden" aria-hidden="true"><label>Leave blank: <input name="bot-field" /></label></p>`
   - Render form fields dynamically from `formFields` array:
     - Fields with `half: true` render in a 2-column grid (`grid grid-cols-1 sm:grid-cols-2 gap-4`)
     - Field with `type: "textarea"` renders as `<textarea>` spanning full width with 4-5 rows
     - Each field has a `<label>` with `for` matching field `id`, and required fields show a red asterisk `<span class="text-red-500">*</span>`
     - Required fields (`email`, `location`) have `required` attribute on the input
     - Each required field has an error `<p>` below it: `<p class="field-error hidden text-sm text-red-600 mt-1" data-for="{field.id}"></p>`
   - Submit button: "Send Message" with yellow styling (`bg-yellow text-navy font-heading font-bold px-8 py-3.5 rounded-full`), `id="submit-btn"`
   - Style inputs with: `w-full px-4 py-3 border border-navy/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal focus:border-transparent` (or similar brand-consistent styling)

   - Wrap the form and success message in a container `<div id="form-wrapper">`:
     - Form element inside
     - Success message div: `<div id="success-msg" class="hidden text-center py-12"><p class="text-xl font-heading font-bold text-navy">Thanks! We'll be in touch within 24 hours.</p></div>`

   - Add `<script>` block with vanilla JS:
     a. **Inline validation on blur:** For each `[required]` input, on blur event, call `validateField()`. Check `validity.valueMissing` (show "This field is required.") and for email type check `validity.typeMismatch` (show "Please enter a valid email address."). Show/hide the corresponding `[data-for]` error element.
     b. **Form submit handler:** `e.preventDefault()`, run `validateForm()` (validates all required fields, returns boolean), if invalid return early. Disable submit button, change text to "Sending...". Use `fetch('/', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams(new FormData(form) as unknown as Record<string, string>).toString() })`. On success: fade form out (`opacity 0` over 400ms transition), then `hidden` class on form, remove `hidden` from success-msg. On error: re-enable button, restore text, show a generic error message below the form.
     c. Use the TypeScript cast `as unknown as Record<string, string>` for FormData → URLSearchParams to avoid TS build errors (per RESEARCH pitfall 5).

2. Create `src/pages/contact.astro` (page composition):
   - Import BaseLayout, ContactHero, ContactForm, ContactSidebar
   - Use BaseLayout with `title="Contact Us - WorkWiser"` and `description="Get in touch with WorkWiser. Submit an inquiry or book a free consultation call."`
   - Inject Calendly widget assets via head slot:
     ```
     <Fragment slot="head">
       <link href="https://calendly.com/assets/external/widget.css" rel="stylesheet" />
       <script is:inline src="https://calendly.com/assets/external/widget.js" type="text/javascript"></script>
     </Fragment>
     ```
     NOTE: Use `is:inline` on the script tag to prevent Astro from bundling the external CDN script. Without `is:inline`, Astro may try to process the external URL.
   - Page structure:
     - `<ContactHero />` full width
     - Below hero: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24` container
     - Inside container: two-column grid `grid grid-cols-1 lg:grid-cols-3 gap-12 lg:gap-16`
       - Left column (2/3 width): `lg:col-span-2` containing `<ContactForm />`
       - Right column (1/3 width): `lg:col-span-1` containing `<ContactSidebar />`
   - Check how `about.astro` and `why-us.astro` are structured for consistency.

3. Create `.planning/phases/05-contact-us-page/ZAPIER-SETUP-GUIDE.md` (CONT-04):
   - Title: "Zapier Setup Guide: Netlify Forms to Google Sheets"
   - Step-by-step instructions with numbered steps:
     1. Log in to zapier.com (create account if needed)
     2. Click "Create" / "Make a Zap"
     3. Trigger: Search for "Netlify" app, select "New Form Submission" event
     4. Connect Netlify account (OAuth), select the WorkWiser site, select form name "contact"
     5. Test trigger — submit a test form on the Netlify preview URL first
     6. Action: Search for "Google Sheets" app, select "Create Spreadsheet Row" event
     7. Connect Google account, select target spreadsheet and worksheet/tab
     8. Map fields: first_name -> Column A, last_name -> Column B, job_title -> Column C, company_name -> Column D, email -> Column E, location -> Column F, message -> Column G
     9. Test action — verify row appears in Google Sheet
     10. Name the Zap (e.g., "WorkWiser Contact Form -> Google Sheets") and turn it on
   - Also include a section on Netlify email notification setup:
     1. Go to Netlify dashboard -> Site -> Integrations -> Emails & Webhooks (or Site configuration -> Notifications)
     2. Add notification: "Form submission" -> email notification
     3. Select form: "contact"
     4. Set recipient: info@workwiser.io
     5. Save
   - Note: Mention that Zapier free tier allows 100 tasks/month (sufficient for B2B contact form volume)
  </action>
  <verify>
    <automated>cd "C:\Users\david\OneDrive\Documents\Projects\Build WorkWiser Website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ContactForm.astro renders 7 form fields with Netlify Forms attributes, honeypot, AJAX fetch submission, inline validation on blur/submit, and fade-to-success-message behavior. contact.astro composes hero + form + sidebar in two-column layout with Calendly widget loaded via head slot. ZAPIER-SETUP-GUIDE.md provides complete Netlify email notification and Zapier/Sheets setup instructions. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Contact Us page</name>
  <files>src/pages/contact.astro</files>
  <action>
Run `npm run build` followed by `npm run preview` to start the local preview server. Present the verification checklist to the user for visual and functional review of the complete Contact Us page at http://localhost:4321/contact.
  </action>
  <verify>
    <automated>cd "C:\Users\david\OneDrive\Documents\Projects\Build WorkWiser Website" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually verified and approved the Contact Us page: hero renders correctly, all 7 form fields present with validation, sidebar stats count up, contact info is clickable, Calendly popup opens, and mobile layout stacks properly.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `/contact` route is accessible via `npm run preview`
- Form has `data-netlify="true"`, `netlify-honeypot="bot-field"`, and hidden `form-name` field
- Calendly widget.js loads only on contact page (check network tab on home page — should NOT load there)
- ZAPIER-SETUP-GUIDE.md exists with complete Netlify + Zapier instructions
</verification>

<success_criteria>
Contact page renders with hero, two-column form+sidebar layout, all 7 form fields, inline validation, Netlify Forms attributes, Calendly popup, stats with count-up, and contact info. User has approved the visual/functional checkpoint. Zapier setup guide is ready for the user to follow post-deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-us-page/05-02-SUMMARY.md`
</output>
