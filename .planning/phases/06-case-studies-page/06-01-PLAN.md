---
phase: 06-case-studies-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/caseStudies.ts
  - src/components/CaseStudiesHero.astro
  - src/components/CaseStudiesGrid.astro
  - src/components/CaseStudiesCta.astro
  - src/pages/case-studies.astro
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-03
  - CASE-04

must_haves:
  truths:
    - "Visiting /case-studies shows 4 case study cards with client name, industry, challenge, role type, and a measurable outcome"
    - "Each card has a visually prominent outcome stat (large text, accent color) that a visitor notices immediately"
    - "A CTA at the bottom of the page links to /contact"
    - "Adding a 5th case study to caseStudies.ts automatically renders a 5th card with no other code changes"
  artifacts:
    - path: "src/data/caseStudies.ts"
      provides: "CaseStudy interface and 4 placeholder case studies"
      exports: ["CaseStudy", "caseStudies"]
    - path: "src/components/CaseStudiesHero.astro"
      provides: "Teal-to-navy gradient hero matching inner page pattern"
      min_lines: 15
    - path: "src/components/CaseStudiesGrid.astro"
      provides: "2-col responsive grid rendering case study cards"
      min_lines: 20
    - path: "src/components/CaseStudiesCta.astro"
      provides: "Navy gradient CTA section linking to /contact"
      min_lines: 10
    - path: "src/pages/case-studies.astro"
      provides: "Page assembly importing all components"
      min_lines: 10
  key_links:
    - from: "src/components/CaseStudiesGrid.astro"
      to: "src/data/caseStudies.ts"
      via: "import { caseStudies } from '../data/caseStudies'"
      pattern: "import.*caseStudies.*from.*data/caseStudies"
    - from: "src/pages/case-studies.astro"
      to: "src/components/CaseStudiesHero.astro"
      via: "component import"
      pattern: "import CaseStudiesHero"
    - from: "src/components/CaseStudiesCta.astro"
      to: "/contact"
      via: "anchor href"
      pattern: 'href="/contact"'
---

<objective>
Build the Case Studies listing page at /case-studies with 4 placeholder case studies (healthcare, real estate, tech support, customer support) displayed as cards in a brand-consistent 2-column grid.

Purpose: Give prospective clients social proof through named client outcomes, building trust and reinforcing WorkWiser's credibility before they reach the Contact page.
Output: A fully functional /case-studies route with hero, card grid, and bottom CTA — all driven by a TypeScript data file for easy content updates.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-case-studies-page/06-CONTEXT.md
@.planning/phases/06-case-studies-page/06-RESEARCH.md

@src/components/ContactHero.astro
@src/components/Differentiators.astro
@src/data/whyUsContent.ts
@src/pages/contact.astro

<interfaces>
<!-- Existing patterns to clone. Executor should reference these directly. -->

From src/data/whyUsContent.ts (data file pattern):
```typescript
export interface Differentiator {
  title: string;
  description: string;
  iconPath: string;
}
export const differentiators: Differentiator[] = [ ... ];
```

From src/components/ContactHero.astro (hero pattern):
- Full-width section with `bg-gradient-to-br from-teal via-blue to-navy py-24 md:py-32`
- W-arrow SVG watermark (absolute positioned, 10% opacity)
- Centered h1 + subtitle + optional CTA button

From src/components/Differentiators.astro (card grid pattern):
- Responsive grid: `grid md:grid-cols-2 lg:grid-cols-3 gap-8`
- Cards: `rounded-2xl` with padding, icon, title, description

From src/layouts/BaseLayout.astro:
```typescript
// Props: title, description
<BaseLayout title="Page Title" description="Meta description">
  <slot />  <!-- page content -->
</BaseLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create caseStudies.ts data file and all three components</name>
  <files>src/data/caseStudies.ts, src/components/CaseStudiesHero.astro, src/components/CaseStudiesGrid.astro, src/components/CaseStudiesCta.astro</files>
  <action>
    **1. Create `src/data/caseStudies.ts`** with a typed CaseStudy interface and 4 exported case studies:

    Interface fields: `id` (string), `industry` (string), `companyName` (string), `roleType` (string), `challenge` (string — 1-2 sentences), `outcome` (string — 1-2 sentences), `outcomeStat` (string — short metric like "Saved 30 hrs/week"), `iconPath` (string — SVG d-attribute for 24x24 viewBox).

    Case study content (from RESEARCH.md recommendations):
    - Healthcare: Meridian Medical Group, Medical Administrative VA, "Saved 30 hrs/week" — scheduling/follow-up overhead eliminated, no-show rate fell 40%
    - Real Estate: Bayside Property Group, Real Estate Transaction VA, "25% more closes" — transaction coordination freed agent to close more deals
    - Tech Support: ClearPath Technologies, Level 1 Tech Support VA, "88% tickets resolved" — L1 triage handled by VA team, engineers reclaimed dev time
    - Customer Support: Nexus Logistics Co., Customer Support VA, "CSAT: 71% to 94%" — response time cut from 6hrs to 45min

    Use Heroicons 24x24 outline paths for iconPath (heart for healthcare, building-office for real estate, computer-desktop for tech, chat-bubble for customer support). Reference existing iconPath patterns in homeContent.ts or whyUsContent.ts.

    **2. Create `src/components/CaseStudiesHero.astro`** — Clone ContactHero.astro structure exactly (gradient, W-arrow watermark SVG, centered text). Changes:
    - h1: "Real Results from Real Clients" (with "Real Clients" wrapped in `<span class="text-yellow">`)
    - Subtitle: "See how WorkWiser virtual assistants have helped businesses across industries save time, cut costs, and grow faster."
    - No CTA button in hero (the page itself is the proof section; CTA is at the bottom)

    **3. Create `src/components/CaseStudiesGrid.astro`** — Import `caseStudies` from data file. Render a 2-column responsive grid (`grid md:grid-cols-2 gap-8`) inside a `max-w-7xl` container with `py-16 md:py-24` on white background. Each card is an `<article>` element with:
    - Industry pill tag: `inline-block bg-teal/10 text-teal text-xs font-semibold uppercase tracking-wide px-3 py-1 rounded-full`
    - Role type as subtle gray text next to the pill
    - Company name as `h3` in navy, font-heading bold
    - Outcome stat: `text-3xl font-heading font-bold text-teal` — visually prominent (CASE-02 requirement)
    - Challenge paragraph with "Challenge:" label in navy bold
    - Outcome paragraph with "Result:" label in navy bold
    - Card styling: `rounded-2xl bg-gray-50 hover:shadow-md transition-shadow p-6 lg:p-8`
    - Include the SVG icon (24x24 viewBox, stroke-based) near the industry pill or company name area

    **4. Create `src/components/CaseStudiesCta.astro`** — A slim navy gradient CTA section (NOT reusing CtaBanner — it is hardcoded with photo/pricing/Calendly). Use `bg-gradient-to-br from-navy via-navy to-blue py-16 md:py-20`. Centered text:
    - h2: "Ready to get started?" (white, font-heading bold)
    - Subtitle: "Tell us about your business and let's find the right virtual assistant for your team." (white/80)
    - Yellow rounded-full button linking to `/contact` with text "Contact Us"
    - href MUST be "/contact" (not Calendly, not /case-studies — per CASE-04)
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files exist with correct TypeScript types, 4 case studies exported, hero/grid/CTA components render without type errors</done>
</task>

<task type="auto">
  <name>Task 2: Assemble case-studies.astro page and verify build</name>
  <files>src/pages/case-studies.astro</files>
  <action>
    Create `src/pages/case-studies.astro` following the established page assembly pattern (see contact.astro, about.astro):

    ```astro
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import CaseStudiesHero from '../components/CaseStudiesHero.astro';
    import CaseStudiesGrid from '../components/CaseStudiesGrid.astro';
    import CaseStudiesCta from '../components/CaseStudiesCta.astro';
    ---
    <BaseLayout
      title="Case Studies - WorkWiser"
      description="See how WorkWiser virtual assistants have delivered measurable results for clients in healthcare, real estate, tech support, and customer support."
    >
      <CaseStudiesHero />
      <CaseStudiesGrid />
      <CaseStudiesCta />
    </BaseLayout>
    ```

    After creating the file, run `npx astro build` to confirm the full site builds without errors and the /case-studies route is included in the build output.

    Do NOT add a Case Studies link to the Header navigation — this is explicitly deferred to the Brand and SEO phase per CONTEXT.md deferred decisions.
  </action>
  <verify>
    <automated>cd "C:/Users/david/OneDrive/Documents/Projects/Build WorkWiser Website" && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>Full site builds successfully, /case-studies route is in the build output, page renders hero + 4 cards + CTA linking to /contact</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes with zero errors
2. Build output includes `/case-studies/index.html`
3. `src/data/caseStudies.ts` exports a typed CaseStudy interface and exactly 4 case study objects
4. Each case study card shows: industry pill, company name, role type, challenge, outcome, and a visually prominent outcome stat
5. Bottom CTA links to `/contact` (not Calendly)
6. No changes to Header.astro or other existing components
</verification>

<success_criteria>
- /case-studies renders 4 case study cards in a 2-column responsive grid
- Each card displays all CASE-02 fields: industry, role type, measurable outcome stat
- Outcome stat has prominent visual treatment (large teal text)
- Bottom CTA links to /contact page
- Adding a new object to caseStudies array automatically renders a new card
- Site builds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-case-studies-page/06-01-SUMMARY.md`
</output>
